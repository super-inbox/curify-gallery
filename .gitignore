.DS_Store
.DS_Store

# client production vector files — publish a redacted raster instead (see designAI_manufacturing/README.md)
designAI_manufacturing/**/*.ai

# 2026-08-28: supplier quote, 232 MB — exceeds GitHub's 100 MB HARD limit, so it
# can never be pushed. Kept on disk as reference; excluded from git deliberately.
# If it must be versioned, it needs Git LFS (not installed in this repo).
# 2026-09-22: client_VC_portfolio moved out to curify-gtm entirely. Rule kept there.

# 2026-09-22: client-sensitive material moved to curify-gtm. Belt-and-braces so it
# cannot be re-added here by accident — this repo is intended to go public.
client_VC_portfolio/
Curify_GTM/

# 2026-09-09: two cultural_videos files exceed GitHub's 100 MB HARD limit, so any
# commit containing them is unpushable. Same treatment as the supplier quote
# above. See cultural_videos/README.md.
cultural_videos/wuG9jPf5SCrBtez7.mp4
cultural_videos/创意西安v1.m4v
