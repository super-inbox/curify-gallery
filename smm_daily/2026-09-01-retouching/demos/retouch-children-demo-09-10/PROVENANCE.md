# Children's portrait retouching demo — 2026-09-10

Replaces `raw/leads-gen-09-10/retouching-studio.png`, which cannot be posted: it is a re-export of
`raw/context-enrichment-08-31/ai-gen.png`, rendered to **client-008's spec using their 关键词模板**
during an **active competitive 试稿 against another vendor**.

## Provenance

- Generated end-to-end from our own prompt (`gen_retouch_sets.py`, prompt inline).
- `00-source.png` is **synthetic**. No real child appears in any frame, and nothing here derives
  from a client's material.
- The four variants were produced by feeding `00-source.png` BACK IN and changing only the
  backdrop. Two independently generated images would be two different children, which proves
  nothing about a locked subject — and locked subject is the entire claim.
- Standard clothed commercial school-portrait framing throughout, which is what this ICP sells.

## What it shows

Same child, same jumper and trousers, same pose; four backdrops — white sweep, autumn park,
library, festive winter. `_sheet-children.jpg` is the single postable file.

## Still true

No delivered-outcome claim attaches to this. It shows what we can run, not what a client accepted.
The sheet carries the line "AI-generated retouching concepts".
