# Fashion & portrait retouching demo — 2026-09-10

Replaces `raw/leads-gen-09-10/retouching-ai.jpg`, which cannot be posted: it is a the apparel studio CLIENT's own photographs (`raw/studio-retouching-09-02/`), with no recorded permission to publish.
during an **active competitive 试稿 against another vendor**.

## Provenance

- Generated end-to-end from our own prompt (`gen_retouch_sets.py`, prompt inline).
- `00-source.png` is **synthetic**. No real person appears in any frame, and nothing here derives
  from a client's material.
- The four variants were produced by feeding `00-source.png` BACK IN and changing only the
  backdrop. Two independently generated images would be two different people, which proves
  nothing about a locked subject — and locked subject is the entire claim.

## What it shows

Same child, same beige blazer, white shirt and navy trousers, same pose; four backdrops — white sweep, grey editorial, city street, warm interior. `_sheet-fashion.jpg` is the single postable file.

## Still true

No delivered-outcome claim attaches to this. It shows what we can run, not what a client accepted.
The sheet carries the line "AI-generated retouching concepts".
